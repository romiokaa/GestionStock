const API_URL = 'https://ton-serveur.com/api'; // Remplace par l'URL de ton API

export async function login(email, password) {
  // ... (code pour envoyer une requête POST à /api/login)
}

export async function signup(userData) {
  // ... (code pour envoyer une requête POST à /api/signup)
}

export async function getProducts(categories) {
  // ... (code pour envoyer une requête GET à /api/products avec les catégories en paramètres)
}

export async function getProductById(productId) {
  // ... (code pour envoyer une requête GET à /api/products/:id)
}

// ... (autres fonctions pour les interactions "intéressé" et "acheter")
